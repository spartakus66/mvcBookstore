@model IEnumerable<Biblioteka.Models.Borrow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookCopy.BookCopyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Pesel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reader.Pesel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BorrowDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReturnDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookCopy.BookCopyID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Pesel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reader.Pesel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BorrowDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReturnDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BorrowId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BorrowId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BorrowId })
        </td>
    </tr>
}

</table>
